{
  "name": "youtubementries",
  "version": "1.0.0",
  "private": true,
  "description": "A streaming site for documentaries and docuseries.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check": "node scripts/check.mjs"
  }
}
